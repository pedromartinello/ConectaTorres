# Git - v0.5

Depois de testar a versão:

```powershell
git status
git add .
git status
git commit -m "feat: adiciona busca inteligente com IA e recuperacao de senha por email"
git push
```

Depois marque a versão:

```powershell
git tag -a v0.5 -m "ConectaTorres v0.5 - IA e recuperacao de senha por email"
git push origin v0.5
```

Conferência:

```powershell
git status
git tag
```
