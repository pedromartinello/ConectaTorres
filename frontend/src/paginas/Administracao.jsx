import { useEffect, useState } from 'react';
import { api } from '../servicos/api.js';

function fmt(v){return new Intl.DateTimeFormat('pt-BR',{dateStyle:'short'}).format(new Date(v));}
export function Administracao(){const[aba,setAba]=useState('resumo');const[resumo,setResumo]=useState({});const[usuarios,setUsuarios]=useState([]);const[categorias,setCategorias]=useState([]);const[denuncias,setDenuncias]=useState([]);const[avaliacoes,setAvaliacoes]=useState([]);const[novaCategoria,setNovaCategoria]=useState('');const[erro,setErro]=useState('');async function carregar(){try{const[r,u,c,d,a]=await Promise.all([api('/admin/resumo'),api('/admin/usuarios'),api('/admin/categorias'),api('/admin/denuncias'),api('/admin/avaliacoes')]);setResumo(r.resumo);setUsuarios(u.usuarios);setCategorias(c.categorias);setDenuncias(d.denuncias);setAvaliacoes(a.avaliacoes);}catch(e){setErro(e.message);}}useEffect(()=>{carregar();},[]);async function ativo(u){await api(`/admin/usuarios/${u.id}/ativo`,{method:'PATCH',body:JSON.stringify({ativo:!u.ativo})});await carregar();}async function criarCat(e){e.preventDefault();try{await api('/admin/categorias',{method:'POST',body:JSON.stringify({nome:novaCategoria})});setNovaCategoria('');await carregar();}catch(x){setErro(x.message);}}async function toggleCat(c){await api(`/admin/categorias/${c.id}`,{method:'PATCH',body:JSON.stringify({ativa:!c.ativa})});await carregar();}async function statusDenuncia(d,status){await api(`/admin/denuncias/${d.id}`,{method:'PATCH',body:JSON.stringify({status,respostaAdmin:d.respostaAdmin||null})});await carregar();}async function visibilidade(a){await api(`/admin/avaliacoes/${a.id}/visibilidade`,{method:'PATCH',body:JSON.stringify({visivel:!a.visivel})});await carregar();}const abas=[['resumo','Resumo'],['usuarios','Usuarios'],['categorias','Categorias'],['denuncias','Denuncias'],['avaliacoes','Avaliacoes']];return <div><div className="titulo-secao"><span className="rotulo">Administrador</span><h1>Administracao</h1><p className="texto-suave">Gerenciamento essencial da plataforma.</p></div>{erro&&<div className="alerta erro">{erro}</div>}<div className="abas">{abas.map(([id,n])=><button key={id} className={aba===id?'ativo':''} onClick={()=>setAba(id)}>{n}{id==='denuncias'&&resumo.denunciasAbertas?` (${resumo.denunciasAbertas})`:''}</button>)}</div>{aba==='resumo'&&<div className="grade-metricas"><div className="metrica"><span>Usuarios</span><strong>{resumo.usuarios||0}</strong></div><div className="metrica"><span>Clientes ativos</span><strong>{resumo.clientes||0}</strong></div><div className="metrica"><span>Prestadores ativos</span><strong>{resumo.prestadores||0}</strong></div><div className="metrica"><span>Agendamentos</span><strong>{resumo.agendamentos||0}</strong></div><div className="metrica"><span>Denuncias abertas</span><strong>{resumo.denunciasAbertas||0}</strong></div><div className="metrica"><span>Avaliacoes publicas</span><strong>{resumo.avaliacoes||0}</strong></div></div>}{aba==='usuarios'&&<div className="tabela-responsiva"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Tipo</th><th>Cadastro</th><th>Status</th><th></th></tr></thead><tbody>{usuarios.map(u=><tr key={u.id}><td>{u.nome}</td><td>{u.email}</td><td>{u.tipo}</td><td>{fmt(u.createdAt)}</td><td><span className={`status ${u.ativo?'status-aceito':'status-cancelado'}`}>{u.ativo?'ativo':'inativo'}</span></td><td><button className="botao-secundario botao-pequeno" onClick={()=>ativo(u)}>{u.ativo?'Desativar':'Ativar'}</button></td></tr>)}</tbody></table></div>}{aba==='categorias'&&<><form className="formulario-inline" onSubmit={criarCat}><input required minLength="2" value={novaCategoria} onChange={e=>setNovaCategoria(e.target.value)} placeholder="Nova categoria"/><button className="botao">Adicionar</button></form><div className="lista-simples">{categorias.map(c=><div key={c.id} className="linha-gerencial"><div><strong>{c.nome}</strong><span className="texto-suave bloco">{c.slug}</span></div><button className="botao-secundario botao-pequeno" onClick={()=>toggleCat(c)}>{c.ativa?'Desativar':'Ativar'}</button></div>)}</div></>}{aba==='denuncias'&&<div className="lista-simples">{denuncias.map(d=><article key={d.id} className="linha-gerencial"><div><div className="tags"><span className={`status status-${d.status==='resolvida'?'aceito':d.status==='arquivada'?'cancelado':'pendente'}`}>{d.status}</span></div><h3>{d.motivo}</h3><p>{d.descricao||'Sem detalhes.'}</p><p className="texto-suave">Denunciante: {d.denunciante?.nome} • Prestador: {d.prestador?.nome||'-'} • {fmt(d.createdAt)}</p></div><div className="acoes-coluna"><button className="botao-secundario botao-pequeno" onClick={()=>statusDenuncia(d,'em_analise')}>Em analise</button><button className="botao botao-pequeno" onClick={()=>statusDenuncia(d,'resolvida')}>Resolver</button><button className="botao-secundario botao-pequeno" onClick={()=>statusDenuncia(d,'arquivada')}>Arquivar</button></div></article>)}{!denuncias.length&&<div className="vazio">Nenhuma denuncia registrada.</div>}</div>}{aba==='avaliacoes'&&<div className="lista-simples">{avaliacoes.map(a=><article key={a.id} className="linha-gerencial"><div><div className="estrelas">{'★'.repeat(a.nota)}{'☆'.repeat(5-a.nota)}</div><p>{a.comentario||'Sem comentario.'}</p><span className="texto-suave">{a.cliente?.nome} → {a.prestador?.nome} • {fmt(a.createdAt)}</span></div><button className="botao-secundario botao-pequeno" onClick={()=>visibilidade(a)}>{a.visivel?'Ocultar':'Publicar'}</button></article>)}</div>}</div>}
